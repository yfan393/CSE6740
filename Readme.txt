1. eigenface:

    We need to read and resize the images here, so please make sure to install
the necessary PIL and scikit-image packages. 

2. faces_isomap:

    The original data is based on matlab v7.3 files, so please install the 
h5py package in order to read that. 

3. swiss_roll:

    As we need to draw the 3-D plot, so please update the matplotlib package to 
the newest edition to enable 3-D plot.(mpl_toolkits.mplot3d) 

4. USPS_Digits:

    The original data is based on matlab v7.3 files, so please install the 
h5py package in order to read that.